# Alamat dasar backend JSON:API SIMPPM (basis kode terpisah/uncoupled).
# Ganti sesuai lingkungan (dev/staging/production) TANPA mengubah kode --
# sesuai prinsip 12-factor app "Config" (lihat docs/12-FACTOR-APP.md di
# proyek backend). Vite hanya memuat variabel berawalan VITE_ ke bundel.
VITE_API_BASE_URL=http://127.0.0.1:8123/api/v1
